<x-layouts.guest title="Passwort vergessen · cleververein"><section class="cv-panel w-full max-w-md p-7 sm:p-9"><x-logo class="text-xl text-brand-950" /><h1 class="mt-8 text-2xl font-bold">Passwort zurücksetzen</h1><p class="mt-2 text-sm text-slate-500">Wir senden einen zeitlich begrenzten Link an die hinterlegte E-Mail-Adresse.</p>@if(session('status'))<p class="mt-5 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">{{ session('status') }}</p>@endif<form method="post" action="{{ route('password.email') }}" class="mt-6 space-y-5">@csrf<div><label class="cv-label" for="email">E-Mail-Adresse</label><input class="cv-input" id="email" name="email" type="email" value="{{ old('email') }}" required autofocus>@error('email')<p class="mt-1 text-sm text-red-700">{{ $message }}</p>@enderror</div><button class="cv-button-primary w-full">Link anfordern</button><a href="{{ route('login') }}" class="block text-center text-sm font-semibold text-slate-600 hover:underline">Zurück zur Anmeldung</a></form></section></x-layouts.guest>
